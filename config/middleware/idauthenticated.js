//middleware that restricts user access
module.exports = function(req, res, next) {
  //if there is a user allow them to access w.e routes they are heading
  // if there is no user logged in redirect them to login
};
